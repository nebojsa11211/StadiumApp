# Docker Environment Variables Template
# Copy this file to .env.docker and update with your actual values
# DO NOT commit .env.docker to version control

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL/Supabase Connection String
# Format: Host=your-host;Port=5432;Database=your-db;Username=your-user;Password=your-password;SSL Mode=Require;Trust Server Certificate=true
DB_CONNECTION_STRING=Host=aws-1-eu-north-1.pooler.supabase.com;Port=6543;Database=postgres;Username=postgres.ylaccqjfrqzruhgbzpnw;Password=your-supabase-password;SSL Mode=Require;Trust Server Certificate=true;Timeout=60;Command Timeout=300;Keepalive=30;Connection Idle Lifetime=300;Maximum Pool Size=50;Minimum Pool Size=5;Pooling=true;No Reset On Close=true;Include Error Detail=true

# =============================================================================
# JWT AUTHENTICATION SETTINGS
# =============================================================================
# JWT Secret Key (minimum 32 characters)
JWT_SECRET_KEY=YourSuperSecretKeyThatIsAtLeast32CharactersLongForProduction!
JWT_ISSUER=StadiumDrinkOrdering
JWT_AUDIENCE=StadiumDrinkOrdering

# =============================================================================
# SSL CERTIFICATE CONFIGURATION
# =============================================================================
# Certificate password for aspnetcore.pfx
SSL_CERT_PASSWORD=YourSecureCertPassword123!

# =============================================================================
# APPLICATION ENVIRONMENT
# =============================================================================
# Environment: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Development

# Timezone for all containers
TZ=Europe/Zagreb

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================
# Internal API base URL for container-to-container communication
API_BASE_URL=https://api:8443/

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Log levels: Trace, Debug, Information, Warning, Error, Critical
LOG_LEVEL_DEFAULT=Information
LOG_LEVEL_ASPNETCORE=Information

# =============================================================================
# PRODUCTION SECURITY NOTES
# =============================================================================
# For production deployment:
# 1. Use strong, unique passwords for all services
# 2. Generate a new JWT secret key (use: openssl rand -base64 32)
# 3. Use production SSL certificates from a trusted CA
# 4. Set ASPNETCORE_ENVIRONMENT=Production
# 5. Configure proper database connection with production credentials
# 6. Review and adjust log levels appropriately
# 7. Enable proper firewall and network security