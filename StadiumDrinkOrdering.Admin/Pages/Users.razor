@page "/users"
@using StadiumDrinkOrdering.Shared.Models
@inject IStringLocalizer<SharedResources> L

<PageTitle>@L["Users_PageTitle"]</PageTitle>

<div class="console" id="admin-users-main-container">
    <!-- Page Header -->
    <header class="console-head" id="admin-users-header">
        <div class="console-head__title" id="admin-users-title-section">
            <span class="eyebrow">@L["Users_EyebrowLabel"]</span>
            <h1>@L["Users_MainTitle"]</h1>
            <p>@L["Users_Subtitle"]</p>
        </div>
        <div class="console-head__actions" id="admin-users-action-buttons">
            <button class="pill-btn pill-btn--primary" @onclick="ShowCreateUserModal" id="admin-users-create-btn">
                <i class="oi oi-plus"></i><span>@L["Users_AddUserButtonText"]</span>
            </button>
            <button class="pill-btn pill-btn--success" @onclick="CreateSampleUsers" disabled="@isCreatingSampleUsers" id="admin-users-create-sample-btn">
                <i class="oi oi-people @(isCreatingSampleUsers ? "spin" : "")"></i><span>@L["Users_CreateSampleUsersButtonText"]</span>
            </button>
            <button class="icon-btn" @onclick="RefreshData" disabled="@isLoading" id="admin-users-refresh-btn" title="@L["Users_RefreshButtonTooltip"]">
                <i class="oi oi-reload @(isLoading ? "spin" : "")"></i>
            </button>
        </div>
    </header>

    <!-- Key Metrics Cards -->
    <section class="metrics" id="admin-users-summary-cards">
        <!-- Total Users -->
        <article class="metric" id="admin-users-total-card">
            <div class="metric__head" id="admin-users-total-card-content">
                <span class="metric__label" id="admin-users-total-stats">@L["Users_TotalUsersLabel"]</span>
                <i class="oi oi-people metric__icon" id="admin-users-total-icon"></i>
            </div>
            <div class="metric__value mono" id="admin-users-total-card-inner">@totalUsers</div>
            <div class="metric__foot" id="admin-users-total-card-body">
                <span class="metric__hint">@L["Users_TotalUsersHint"]</span>
            </div>
        </article>

        <!-- Active Users (accent) -->
        <article class="metric metric--accent" id="admin-users-active-card">
            <div class="metric__head" id="admin-users-active-card-content">
                <span class="metric__label" id="admin-users-active-stats">@L["Users_ActiveUsersLabel"]</span>
                <i class="oi oi-circle-check metric__icon is-ok" id="admin-users-active-icon"></i>
            </div>
            <div class="metric__value mono" id="admin-users-active-card-inner">@activeUsers</div>
            <div class="metric__foot" id="admin-users-active-card-body">
                <span class="metric__hint">@L["Users_ActiveUsersHint"]</span>
            </div>
        </article>

        <!-- New This Month -->
        <article class="metric" id="admin-users-new-card">
            <div class="metric__head" id="admin-users-new-card-content">
                <span class="metric__label" id="admin-users-new-stats">@L["Users_NewThisMonthLabel"]</span>
                <i class="oi oi-person metric__icon" id="admin-users-new-icon"></i>
            </div>
            <div class="metric__value mono" id="admin-users-new-card-inner">@newUsersThisMonth</div>
            <div class="metric__foot" id="admin-users-new-card-body">
                <span class="metric__hint">@L["Users_NewThisMonthHint"]</span>
            </div>
        </article>

        <!-- Admin Users -->
        <article class="metric" id="admin-users-admin-card">
            <div class="metric__head" id="admin-users-admin-card-content">
                <span class="metric__label" id="admin-users-admin-stats">@L["Users_AdminUsersLabel"]</span>
                <i class="oi oi-shield metric__icon" id="admin-users-admin-icon"></i>
            </div>
            <div class="metric__value mono" id="admin-users-admin-card-inner">@adminUsers</div>
            <div class="metric__foot" id="admin-users-admin-card-body">
                <span class="metric__hint">@L["Users_AdminUsersHint"]</span>
            </div>
        </article>
    </section>

    <!-- Filters -->
    <div class="panel" id="admin-users-filters-card">
        <div class="panel__head" id="admin-users-filters-body">
            <h2 class="panel__title">@L["Users_FiltersTitle"]</h2>
            <button class="link-btn" @onclick="ClearFilters" id="admin-users-clear-filters-btn">
                <i class="oi oi-x"></i> @L["Users_ClearFiltersButton"]
            </button>
        </div>
        <div class="filters" id="admin-users-filters-row">
            <div class="field" id="admin-users-role-filter-col">
                <label class="field__label">@L["Users_RoleFilterLabel"]</label>
                <select class="field__input" @bind="selectedRole" @bind:after="FilterUsers" id="admin-users-role-filter">
                    <option value="">@L["Users_AllRolesOption"]</option>
                    <option value="Admin">@L["Users_RoleAdmin"]</option>
                    <option value="Bartender">@L["Users_RoleBartender"]</option>
                    <option value="Waiter">@L["Users_RoleWaiter"]</option>
                </select>
            </div>
            <div class="field" id="admin-users-status-filter-col">
                <label class="field__label">@L["Users_StatusFilterLabel"]</label>
                <select class="field__input" @bind="selectedStatus" @bind:after="FilterUsers" id="admin-users-status-filter">
                    <option value="">@L["Users_AllStatusesOption"]</option>
                    <option value="true">@L["Users_ActiveStatusOption"]</option>
                    <option value="false">@L["Users_InactiveStatusOption"]</option>
                </select>
            </div>
            <div class="field field--grow" id="admin-users-search-filter-col">
                <label class="field__label">@L["Users_SearchUsersLabel"]</label>
                <input type="text" class="field__input" placeholder="@L["Users_SearchUsersPlaceholder"]"
                       @bind="searchTerm" @oninput="FilterUsers" id="admin-users-search-input" />
            </div>
        </div>
    </div>

    @if (isLoading)
    {
        <div class="loading-state" id="admin-users-loading-state">
            <span class="loading-spinner" id="admin-users-loading-spinner"></span>
            <p id="admin-users-loading-text">@L["Users_LoadingMessage"]</p>
        </div>
    }
    else
    {
        <!-- Users Table -->
        <div class="panel" id="admin-users-table-panel">
            <div class="panel__head" id="admin-users-table-head">
                <h2 class="panel__title" id="admin-users-table-title">@L["Users_UsersTableTitle"] <span class="panel__count mono" id="admin-users-table-count">@filteredUsers.Count</span></h2>
                @if (selectedUserIds.Any())
                {
                    <div class="bulk-actions" id="admin-users-bulk-actions">
                        <button class="pill-btn pill-btn--success" @onclick="BulkActivate" id="admin-users-bulk-activate-btn">
                            <i class="oi oi-circle-check"></i><span>@L["Users_ActivateButtonText", selectedUserIds.Count]</span>
                        </button>
                        <button class="pill-btn" @onclick="BulkDeactivate" id="admin-users-bulk-deactivate-btn">
                            <i class="oi oi-ban"></i><span>@L["Users_DeactivateButtonText"]</span>
                        </button>
                    </div>
                }
            </div>

            @if (filteredUsers?.Any() == true)
            {
                <table class="console-table" id="admin-users-table">
                    <thead id="admin-users-table-head">
                        <tr id="admin-users-table-header-row">
                            <th class="check" id="admin-users-table-col-checkbox">
                                <input type="checkbox" class="check-box" @onchange="ToggleSelectAll" id="admin-users-select-all-checkbox" />
                            </th>
                            <th class="sortable @(sortState.IsSorted("id") ? "is-sorted" : "")" @onclick='() => SortBy("id")' id="admin-users-table-col-id">@L["Users_UserColumnHeader"]<span class="sort-ind">@sortState.Indicator("id")</span></th>
                            <th class="sortable @(sortState.IsSorted("name") ? "is-sorted" : "")" @onclick='() => SortBy("name")' id="admin-users-table-col-name">@L["Users_NameColumnHeader"]<span class="sort-ind">@sortState.Indicator("name")</span></th>
                            <th class="sortable @(sortState.IsSorted("username") ? "is-sorted" : "")" @onclick='() => SortBy("username")' id="admin-users-table-col-username">@L["Users_UsernameColumnHeader"]<span class="sort-ind">@sortState.Indicator("username")</span></th>
                            <th class="sortable @(sortState.IsSorted("email") ? "is-sorted" : "")" @onclick='() => SortBy("email")' id="admin-users-table-col-email">@L["Users_EmailColumnHeader"]<span class="sort-ind">@sortState.Indicator("email")</span></th>
                            <th class="sortable @(sortState.IsSorted("role") ? "is-sorted" : "")" @onclick='() => SortBy("role")' id="admin-users-table-col-role">@L["Users_RoleColumnHeader"]<span class="sort-ind">@sortState.Indicator("role")</span></th>
                            <th class="sortable @(sortState.IsSorted("status") ? "is-sorted" : "")" @onclick='() => SortBy("status")' id="admin-users-table-col-status">@L["Users_StatusColumnHeader"]<span class="sort-ind">@sortState.Indicator("status")</span></th>
                            <th class="num sortable @(sortState.IsSorted("created") ? "is-sorted" : "")" @onclick='() => SortBy("created")' id="admin-users-table-col-created">@L["Users_CreatedColumnHeader"]<span class="sort-ind">@sortState.Indicator("created")</span></th>
                            <th class="actions-col" id="admin-users-table-col-actions">@L["Users_ActionsColumnHeader"]</th>
                        </tr>
                    </thead>
                    <tbody id="admin-users-table-body">
                        @foreach (var user in filteredUsers.Take(50))
                        {
                            <tr class="is-clickable @(selectedUserIds.Contains(user.Id) ? "is-selected" : "")" @onclick="@(() => ShowUserStats(user))" id="admin-users-row-@user.Id" title="@L["Users_ViewStatsRowTooltip"]">
                                <td class="check" id="admin-users-cell-checkbox-@user.Id" @onclick:stopPropagation="true">
                                    <input type="checkbox" class="check-box"
                                           checked="@selectedUserIds.Contains(user.Id)"
                                           @onchange="@((e) => ToggleUserSelection(user.Id, (bool)e.Value!))" id="admin-users-checkbox-@user.Id" />
                                </td>
                                <td class="mono console-table__id" id="admin-users-cell-id-@user.Id">#@user.Id</td>
                                <td id="admin-users-cell-name-@user.Id">@user.Name</td>
                                <td id="admin-users-cell-username-@user.Id">@user.Username</td>
                                <td class="console-table__muted" id="admin-users-cell-email-@user.Id">@user.Email</td>
                                <td id="admin-users-cell-role-@user.Id">
                                    <span class="badge @GetRoleBadgeClass(user.Role)">@user.Role</span>
                                </td>
                                <td id="admin-users-cell-status-@user.Id">
                                    <span class="status @(user.IsActive ? "is-active" : "is-inactive")">
                                        <span class="status__dot"></span>@(user.IsActive ? L["Users_ActiveStatus"] : L["Users_InactiveStatus"])
                                    </span>
                                </td>
                                <td class="num mono console-table__time" id="admin-users-cell-created-@user.Id">@user.CreatedAt.ToString("MMM dd, yyyy")</td>
                                <td class="actions-col" id="admin-users-cell-actions-@user.Id" @onclick:stopPropagation="true">
                                    <div class="row-actions" id="admin-users-actions-@user.Id">
                                        <button class="row-btn" @onclick="@(() => EditUser(user))" id="admin-users-edit-btn-@user.Id" title="@L["Users_EditUserTitle"]">
                                            <i class="oi oi-pencil"></i>
                                        </button>
                                        <button class="row-btn" @onclick="@(() => ResetPassword(user.Id))" id="admin-users-reset-password-btn-@user.Id" title="@L["Users_ResetPasswordTitle"]">
                                            <i class="oi oi-key"></i>
                                        </button>
                                        @if (user.IsActive)
                                        {
                                            <button class="row-btn" @onclick="@(() => DeactivateUser(user.Id))" id="admin-users-deactivate-btn-@user.Id" title="@L["Users_DeactivateUserTitle"]">
                                                <i class="oi oi-ban"></i>
                                            </button>
                                        }
                                        else
                                        {
                                            <button class="row-btn row-btn--ok" @onclick="@(() => ActivateUser(user.Id))" id="admin-users-activate-btn-@user.Id" title="@L["Users_ActivateUserTitle"]">
                                                <i class="oi oi-circle-check"></i>
                                            </button>
                                        }
                                        @if (CanDeleteUser(user))
                                        {
                                            <button class="row-btn row-btn--danger" @onclick="@(() => DeleteUser(user.Id))" id="admin-users-delete-btn-@user.Id" title="@L["Users_DeleteUserTitle"]">
                                                <i class="oi oi-trash"></i>
                                            </button>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

                @if (filteredUsers.Count > 50)
                {
                    <div class="table-foot" id="admin-users-table-foot">
                        <p id="admin-users-table-foot-message">@L["Users_ShowingFirstMessage"] <span class="mono">50</span> @L["Users_OfTotalMessage"] <span class="mono">@filteredUsers.Count</span> @L["Users_TotalUsersText"]</p>
                    </div>
                }
            }
            else
            {
                <div class="empty" id="admin-users-empty">
                    <i class="oi oi-info" id="admin-users-empty-icon"></i>
                    <p id="admin-users-empty-title">@L["Users_NoUsersFound"]</p>
                    <span id="admin-users-empty-message">@L["Users_NoUsersFiltersMessage"]</span>
                </div>
            }
        </div>
    }
</div>

<!-- Create User Modal -->
@if (showCreateModal)
{
    <div class="modal fade show" id="admin-users-modal" tabindex="-1" role="dialog" style="display: block; z-index: 1055;" aria-modal="true" aria-labelledby="admin-users-modal-title">
        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="admin-users-modal-title">@L["Users_AddNewUserModalTitle"]</h5>
                    <button type="button" class="btn-close" @onclick="CloseCreateModal" id="admin-users-modal-close-btn" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="admin-users-modal-body">
                    <EditForm Model="newUser" OnValidSubmit="CreateUser" id="admin-users-modal-form">
                        <DataAnnotationsValidator />
                        <ValidationSummary />

                        <div class="mb-3" id="admin-users-modal-username-group">
                            <label class="form-label" for="admin-users-modal-username-input">@L["Users_UsernameLabel"] *</label>
                            <InputText class="form-control" @bind-Value="newUser.Username" id="admin-users-modal-username-input" />
                        </div>

                        <div class="mb-3" id="admin-users-modal-email-group">
                            <label class="form-label" for="admin-users-modal-email-input">@L["Users_EmailLabel"] *</label>
                            <InputText class="form-control" @bind-Value="newUser.Email" type="email" id="admin-users-modal-email-input" />
                        </div>

                        <div class="mb-3" id="admin-users-modal-name-group">
                            <label class="form-label" for="admin-users-modal-name-input">@L["Users_FullNameLabel"]</label>
                            <InputText class="form-control" @bind-Value="newUser.FirstName" id="admin-users-modal-name-input" />
                        </div>

                        <div class="mb-3" id="admin-users-modal-password-group">
                            <label class="form-label" for="admin-users-modal-password-input">@L["Users_PasswordLabel"] *</label>
                            <InputText class="form-control" @bind-Value="newUser.Password" type="password" id="admin-users-modal-password-input" />
                        </div>

                        <div class="mb-3" id="admin-users-modal-role-group">
                            <label class="form-label" for="admin-users-modal-role-select">@L["Users_RoleLabel"] *</label>
                            <InputSelect class="form-select" @bind-Value="newUser.Role" id="admin-users-modal-role-select">
                                <option value="@UserRole.Bartender">@L["Users_RoleBartender"]</option>
                                <option value="@UserRole.Waiter">@L["Users_RoleWaiter"]</option>
                                <option value="@UserRole.Admin">@L["Users_RoleAdmin"]</option>
                            </InputSelect>
                        </div>

                        <div class="modal-footer" id="admin-users-modal-footer">
                            <button type="button" class="btn btn-secondary" @onclick="CloseCreateModal" id="admin-users-modal-cancel-btn">@L["Users_CancelButton"]</button>
                            <button type="submit" class="btn btn-primary" disabled="@isCreatingUser" id="admin-users-modal-submit-btn">
                                @if (isCreatingUser)
                                {
                                    <span class="spinner-border spinner-border-sm me-2"></span>
                                }
                                @L["Users_CreateUserButton"]
                            </button>
                        </div>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-backdrop fade show" style="z-index: 1050;" id="admin-users-modal-backdrop"></div>
}

<!-- Edit User Modal -->
@if (showEditModal)
{
    <div class="modal fade show" id="admin-users-edit-modal" tabindex="-1" role="dialog" style="display: block; z-index: 1055;" aria-modal="true" aria-labelledby="admin-users-edit-modal-title">
        <div class="modal-dialog modal-dialog-centered modal-lg" role="document" id="admin-users-edit-modal-dialog">
            <div class="modal-content">
                <div class="modal-header" id="admin-users-edit-modal-header">
                    <h5 class="modal-title" id="admin-users-edit-modal-title">@L["Users_EditUserModalTitle"]</h5>
                    <button type="button" class="btn-close" @onclick="CloseEditModal" id="admin-users-edit-modal-close-btn" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="admin-users-edit-modal-body">
                    <EditForm Model="editUser" OnValidSubmit="UpdateUser" id="admin-users-edit-modal-form">
                        <DataAnnotationsValidator />
                        <ValidationSummary />

                        <div class="mb-3" id="admin-users-edit-modal-username-group">
                            <label class="form-label" for="admin-users-edit-modal-username-input">@L["Users_UsernameLabel"]</label>
                            <InputText class="form-control" @bind-Value="editUser.Username" id="admin-users-edit-modal-username-input" />
                        </div>

                        <div class="mb-3" id="admin-users-edit-modal-email-group">
                            <label class="form-label" for="admin-users-edit-modal-email-input">@L["Users_EmailLabel"]</label>
                            <InputText class="form-control" @bind-Value="editUser.Email" type="email" id="admin-users-edit-modal-email-input" />
                        </div>

                        <div class="row" id="admin-users-edit-modal-name-row">
                            <div class="col-md-6 mb-3" id="admin-users-edit-modal-firstname-col">
                                <label class="form-label" for="admin-users-edit-modal-firstname-input">@L["Users_FirstNameLabel"]</label>
                                <InputText class="form-control" @bind-Value="editUser.FirstName" id="admin-users-edit-modal-firstname-input" />
                            </div>
                            <div class="col-md-6 mb-3" id="admin-users-edit-modal-lastname-col">
                                <label class="form-label" for="admin-users-edit-modal-lastname-input">@L["Users_LastNameLabel"]</label>
                                <InputText class="form-control" @bind-Value="editUser.LastName" id="admin-users-edit-modal-lastname-input" />
                            </div>
                        </div>

                        <div class="mb-3" id="admin-users-edit-modal-phone-group">
                            <label class="form-label" for="admin-users-edit-modal-phone-input">@L["Users_PhoneNumberLabel"]</label>
                            <InputText class="form-control" @bind-Value="editUser.PhoneNumber" id="admin-users-edit-modal-phone-input" />
                        </div>

                        <div class="mb-3" id="admin-users-edit-modal-role-group">
                            <label class="form-label" for="admin-users-edit-modal-role-select">@L["Users_RoleLabel"]</label>
                            <InputSelect class="form-select" @bind-Value="editUser.Role" id="admin-users-edit-modal-role-select">
                                <option value="">@L["Users_SelectRoleOption"]</option>
                                <option value="@UserRole.Bartender">@L["Users_RoleBartender"]</option>
                                <option value="@UserRole.Waiter">@L["Users_RoleWaiter"]</option>
                                <option value="@UserRole.Admin">@L["Users_RoleAdmin"]</option>
                            </InputSelect>
                        </div>

                        <div class="mb-3 form-check" id="admin-users-edit-modal-active-group">
                            <input type="checkbox" class="form-check-input" @bind="isActiveChecked" id="admin-users-edit-modal-active-checkbox" />
                            <label class="form-check-label" for="admin-users-edit-modal-active-checkbox">
                                @L["Users_ActiveCheckboxLabel"]
                            </label>
                        </div>

                        <div class="modal-footer" id="admin-users-edit-modal-footer">
                            <button type="button" class="btn btn-secondary" @onclick="CloseEditModal" id="admin-users-edit-modal-cancel-btn">@L["Users_CancelButton"]</button>
                            <button type="submit" class="btn btn-primary" disabled="@isUpdatingUser" id="admin-users-edit-modal-submit-btn">
                                @if (isUpdatingUser)
                                {
                                    <span class="spinner-border spinner-border-sm me-2"></span>
                                }
                                @L["Users_UpdateUserButton"]
                            </button>
                        </div>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-backdrop fade show" style="z-index: 1050;" id="admin-users-edit-modal-backdrop"></div>
}

<!-- Staff Member Stats Modal -->
@if (showStatsModal)
{
    <div class="modal fade show" id="admin-users-stats-modal" tabindex="-1" role="dialog" style="display: block; z-index: 1055;" aria-modal="true" aria-labelledby="admin-users-stats-modal-title">
        <div class="modal-dialog modal-dialog-centered modal-lg" role="document" id="admin-users-stats-modal-dialog">
            <div class="modal-content">
                <div class="modal-header" id="admin-users-stats-modal-header">
                    <h5 class="modal-title" id="admin-users-stats-modal-title">
                        <i class="oi oi-graph me-2"></i>@L["Users_StatsModalTitle"]
                    </h5>
                    <button type="button" class="btn-close" @onclick="CloseStatsModal" id="admin-users-stats-modal-close-btn" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="admin-users-stats-modal-body">
                    @if (isLoadingStats)
                    {
                        <div class="text-center py-4" id="admin-users-stats-loading">
                            <span class="spinner-border" role="status"></span>
                            <p class="mt-2">@L["Users_StatsLoadingMessage"]</p>
                        </div>
                    }
                    else if (selectedStats == null)
                    {
                        <div class="alert alert-warning" id="admin-users-stats-error">@L["Users_StatsUnavailableMessage"]</div>
                    }
                    else
                    {
                        <!-- Member header -->
                        <div class="d-flex align-items-center mb-4" id="admin-users-stats-member">
                            <div class="flex-grow-1">
                                <h4 class="mb-1" id="admin-users-stats-member-name">@selectedStats.FullName</h4>
                                <div class="text-muted small" id="admin-users-stats-member-meta">
                                    <span class="badge @GetRoleBadgeClass(selectedStats.Role) me-2">@selectedStats.Role</span>
                                    <span class="me-2">@selectedStats.Email</span>
                                    <span class="status @(selectedStats.IsActive ? "is-active" : "is-inactive")">
                                        <span class="status__dot"></span>@(selectedStats.IsActive ? L["Users_ActiveStatus"] : L["Users_InactiveStatus"])
                                    </span>
                                </div>
                            </div>
                        </div>

                        <!-- Order metrics -->
                        <h6 class="text-uppercase text-muted mb-2" id="admin-users-stats-orders-heading">@L["Users_StatsOrdersHeading"]</h6>
                        <div class="row g-3 mb-4" id="admin-users-stats-orders">
                            <div class="col-6 col-md-3">
                                <div class="stat-tile" id="admin-users-stats-total-handled">
                                    <div class="stat-tile__value">@selectedStats.TotalOrdersHandled</div>
                                    <div class="stat-tile__label">@L["Users_StatsTotalHandled"]</div>
                                </div>
                            </div>
                            <div class="col-6 col-md-3">
                                <div class="stat-tile" id="admin-users-stats-accepted">
                                    <div class="stat-tile__value">@selectedStats.OrdersAccepted</div>
                                    <div class="stat-tile__label">@L["Users_StatsAccepted"]</div>
                                </div>
                            </div>
                            <div class="col-6 col-md-3">
                                <div class="stat-tile" id="admin-users-stats-prepared">
                                    <div class="stat-tile__value">@selectedStats.OrdersPrepared</div>
                                    <div class="stat-tile__label">@L["Users_StatsPrepared"]</div>
                                </div>
                            </div>
                            <div class="col-6 col-md-3">
                                <div class="stat-tile" id="admin-users-stats-delivered">
                                    <div class="stat-tile__value">@selectedStats.OrdersDelivered</div>
                                    <div class="stat-tile__label">@L["Users_StatsDelivered"]</div>
                                </div>
                            </div>
                        </div>

                        <!-- Revenue + events -->
                        <div class="row g-3 mb-4" id="admin-users-stats-secondary">
                            <div class="col-6 col-md-3">
                                <div class="stat-tile" id="admin-users-stats-revenue">
                                    <div class="stat-tile__value">@selectedStats.RevenueDelivered.ToString("C")</div>
                                    <div class="stat-tile__label">@L["Users_StatsRevenueDelivered"]</div>
                                </div>
                            </div>
                            <div class="col-6 col-md-3">
                                <div class="stat-tile" id="admin-users-stats-events">
                                    <div class="stat-tile__value">@selectedStats.EventsAssigned</div>
                                    <div class="stat-tile__label">@L["Users_StatsEventsAssigned"]</div>
                                </div>
                            </div>
                            <div class="col-6 col-md-3">
                                <div class="stat-tile" id="admin-users-stats-upcoming-events">
                                    <div class="stat-tile__value">@selectedStats.UpcomingEventsAssigned</div>
                                    <div class="stat-tile__label">@L["Users_StatsUpcomingEvents"]</div>
                                </div>
                            </div>
                            <div class="col-6 col-md-3">
                                <div class="stat-tile" id="admin-users-stats-in-preparation">
                                    <div class="stat-tile__value">@selectedStats.OrdersInPreparation</div>
                                    <div class="stat-tile__label">@L["Users_StatsInPreparation"]</div>
                                </div>
                            </div>
                        </div>

                        <!-- Account timeline -->
                        <h6 class="text-uppercase text-muted mb-2" id="admin-users-stats-account-heading">@L["Users_StatsAccountHeading"]</h6>
                        <ul class="list-unstyled mb-0" id="admin-users-stats-account">
                            <li class="d-flex justify-content-between py-1 border-bottom">
                                <span class="text-muted">@L["Users_StatsMemberSince"]</span>
                                <span>@selectedStats.CreatedAt.ToString("MMM dd, yyyy")</span>
                            </li>
                            <li class="d-flex justify-content-between py-1 border-bottom">
                                <span class="text-muted">@L["Users_StatsLastLogin"]</span>
                                <span>@(selectedStats.LastLoginAt?.ToString("MMM dd, yyyy HH:mm") ?? L["Users_StatsNever"])</span>
                            </li>
                            <li class="d-flex justify-content-between py-1">
                                <span class="text-muted">@L["Users_StatsLastOrderHandled"]</span>
                                <span>@(selectedStats.LastOrderHandledAt?.ToString("MMM dd, yyyy HH:mm") ?? L["Users_StatsNever"])</span>
                            </li>
                        </ul>
                    }
                </div>
                <div class="modal-footer" id="admin-users-stats-modal-footer">
                    <button type="button" class="btn btn-secondary" @onclick="CloseStatsModal" id="admin-users-stats-modal-close-footer-btn">@L["Users_CloseButton"]</button>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-backdrop fade show" style="z-index: 1050;" id="admin-users-stats-modal-backdrop"></div>
}


