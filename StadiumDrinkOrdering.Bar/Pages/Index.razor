@page "/index"
@inject NavigationManager Navigation
@inject IStringLocalizer<SharedResources> Localizer

<PageTitle>@Localizer["Login_StadiumBar"]</PageTitle>

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/", true);
    }
}
