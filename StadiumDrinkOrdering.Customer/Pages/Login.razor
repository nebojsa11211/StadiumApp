@page "/login"
@layout MobileShell
@using StadiumDrinkOrdering.Shared.DTOs
@using StadiumDrinkOrdering.Customer.Services
@inject IApiService Api
@inject ICustomerAuthStateService AuthState
@inject NavigationManager Nav

<PageTitle>Prijava — HALFTIME</PageTitle>

<ScreenHeader Title="Prijava" Subtitle="Pretplatnici" BackHref="/welcome" />

<div class="login-body">
    <div class="login-hero">
        <div class="badge">⭐ Sezona</div>
        <h1 class="display">DOBRO<br /><span class="grad-text">DOŠAO</span></h1>
        <p>Prijavi se da vidiš svoju sljedeću utakmicu i naručuješ na svoje mjesto.</p>
    </div>

    <EditForm Model="this" OnValidSubmit="HandleLogin" class="login-form">
        @if (!string.IsNullOrEmpty(_error))
        {
            <div class="err">@_error</div>
        }

        <div class="field">
            <label>Email</label>
            <input type="email" @bind="_email" @bind:event="oninput" placeholder="ime@email.hr" autocomplete="email" />
        </div>
        <div class="field">
            <label>Lozinka</label>
            <input type="password" @bind="_password" @bind:event="oninput" placeholder="••••••••" autocomplete="current-password" />
        </div>

        <button type="submit" class="btn btn-primary" disabled="@_busy">
            @if (_busy)
            {
                <span class="spinner"></span> <span>Prijavljujem…</span>
            }
            else
            {
                <span>Prijavi se</span>
            }
        </button>
    </EditForm>

    <p class="alt">Došao si samo na ovu utakmicu? <a @onclick="@(() => Nav.NavigateTo("/scan"))">Skeniraj ulaznicu</a>.</p>
</div>

@code {
    private string _email = "";
    private string _password = "";
    private string? _error;
    private bool _busy;
    private string? _returnUrl;

    protected override void OnInitialized()
    {
        var q = System.Web.HttpUtility.ParseQueryString(new Uri(Nav.Uri).Query);
        _returnUrl = q["returnUrl"];
        if (AuthState.IsAuthenticated)
            Nav.NavigateTo(string.IsNullOrEmpty(_returnUrl) ? "/home" : _returnUrl);
    }

    private async Task HandleLogin()
    {
        if (_busy) return;
        _busy = true;
        _error = null;

        try
        {
            var result = await Api.LoginAsync(new LoginDto { Email = _email, Password = _password });
            if (result?.Token != null && result.User != null)
            {
                await AuthState.LoginAsync(result.Token, result.User);
                Nav.NavigateTo(string.IsNullOrEmpty(_returnUrl) ? "/home" : _returnUrl, forceLoad: false);
            }
            else
            {
                _error = "Neispravan email ili lozinka.";
            }
        }
        catch (HttpRequestException)
        {
            _error = "Nije moguće spojiti se na poslužitelj.";
        }
        catch (Exception)
        {
            _error = "Došlo je do pogreške. Pokušaj ponovno.";
        }
        finally
        {
            _busy = false;
        }
    }
}
