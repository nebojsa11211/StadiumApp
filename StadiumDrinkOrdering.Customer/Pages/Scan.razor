@page "/scan"
@layout MobileShell
@inject NavigationManager Nav

<PageTitle>Skeniraj ulaznicu — HALFTIME</PageTitle>

<ScreenHeader Title="Skeniraj ulaznicu" BackHref="/welcome" />

<div class="scan-body">
    <div class="reticle">
        <span class="corner tl"></span><span class="corner tr"></span>
        <span class="corner bl"></span><span class="corner br"></span>
        <div class="qr-emoji">🎟️</div>
        <div class="scanline"></div>
    </div>

    <p class="scan-hint">Uperi kameru svog telefona u QR kôd na ulaznici — aplikacija se otvara automatski.</p>

    <div class="manual">
        <div class="label">Ne možeš skenirati?</div>
        <div class="manual-row">
            <input @bind="_code" @bind:event="oninput" placeholder="Unesi kôd s ulaznice" @onkeydown="OnKey" />
            <button class="btn btn-primary btn-sm" @onclick="Submit" disabled="@string.IsNullOrWhiteSpace(_code)">Nastavi</button>
        </div>
        @if (_error)
        {
            <div class="err">Unesi važeći kôd s ulaznice.</div>
        }
    </div>
</div>

@code {
    private string _code = "";
    private bool _error;

    private void OnKey(KeyboardEventArgs e)
    {
        if (e.Key == "Enter") Submit();
    }

    private void Submit()
    {
        if (string.IsNullOrWhiteSpace(_code)) { _error = true; return; }
        Nav.NavigateTo($"/t/{_code.Trim()}");
    }
}
